monitor.alsa.rules = [
     {
       matches = [
         {
           node.name = "~alsa_input.*"
         }
       ]
       actions = {
         update-properties = {
           session.suspend-on-idle = false
         }
       }
     }
   ]
